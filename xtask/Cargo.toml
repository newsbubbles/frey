[package]
name = "xtask"
description = "Repository checks that are not tests: the producer lint and the claims table."
version.workspace = true
edition.workspace = true
license.workspace = true
publish = false

[lints]
workspace = true

[dependencies]
toml = "0.9"
serde = { workspace = true }
serde_json = { workspace = true }
