[package]
name = "frey-core"
description = "Core domain types for the Frey agent framework: items, taint labels, capabilities, and errors. No I/O."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
smol_str.workspace = true
futures-core.workspace = true
dynosaur.workspace = true

[dev-dependencies]
# A three-line block_on, so frey-core can test async traits without taking a runtime
# dependency and without hand-rolling an executor that would need `unsafe`.
pollster = "0.4"

[lints]
workspace = true
