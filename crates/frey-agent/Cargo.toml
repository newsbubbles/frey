[package]
name = "frey-agent"
description = "The Frey agent loop: run journal, deterministic replay, usage ledger, and tracing."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
frey-core = { path = "../frey-core", version = "0.1.1" }
frey-context = { path = "../frey-context", version = "0.1.1" }
frey-tools = { path = "../frey-tools", version = "0.1.1" }
serde.workspace = true
serde_json.workspace = true
smol_str.workspace = true
thiserror.workspace = true
tracing = "0.1"

[dev-dependencies]
frey-testkit = { path = "../frey-testkit", version = "0.1.1" }
pollster = "0.4"

[lints]
workspace = true
